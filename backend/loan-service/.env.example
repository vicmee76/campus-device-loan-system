# Loan Service Environment Variables

# Server Configuration
PORT=7779
NODE_ENV=development

# Database Configuration
# Use DATABASE_URL format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/campus_device_loan

# JWT Configuration (MUST match Device Service)
# This service validates JWT tokens issued by the Device Service
# Both services must use the same JWT_SECRET
JWT_SECRET=your-shared-secret-key-here
JWT_EXPIRES_IN=24h

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Logging Configuration
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARN, ERROR
# Default: DEBUG (if not set)

# Email Service Testing (Optional)
# Set to 'true' to simulate email failures for testing
# SIMULATE_EMAIL_FAILURE=false
